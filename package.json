{
  "name": "bigfront-academy",
  "version": "1.0.0",
  "private": true,
  "description": "大前端打怪升级学习平台：框架网页 -> 课程包 -> 课程与作业",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,green \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "start": "npm --prefix server run start",
    "build": "npm --prefix web run build",
    "install:all": "npm install && npm --prefix server install && npm --prefix web install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
