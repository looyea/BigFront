# TS L2 作业 · 接口

1. 定义 `interface Product`：`id` 只读、`title` 必填、`price?` 可选。
2. 用 `type` 表示一个"颜色"联合：red|green|blue。
3. 写一个函数类型 `Comparator`，接收两个 number 返回 number。
4.（挑战）说明 interface 声明合并，写两个同名 interface 观察属性叠加。
