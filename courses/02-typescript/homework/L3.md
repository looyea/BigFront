# TS L3 作业 · 联合与收窄

1. 写函数 `format(v: string | number)`，分别按类型处理。
2. 用可辨识联合建模 HTTP 状态：loading / success / error，并写 `describe()` 处理每种。
3. 加入 `assertNever` 做穷尽检查，故意漏写一个分支看是否报错。
4.（挑战）自定义类型守卫函数 `function isDog(x: Animal): x is Dog`。
