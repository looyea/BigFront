# TS L1 作业 · 类型基础

1. 给 `function add(a,b){return a+b}` 加上参数与返回值类型注解。
2. 声明一个 `Record<string, number>` 存 3 个学生分数并遍历打印。
3. 说明 `any` 与 `unknown` 的区别，并各写一段示例（unknown 需要收窄才能使用）。
4.（挑战）用 `catch (e: unknown)` 处理异常，安全打印错误信息（提示：收窄为 Error 后用 e.message）。
