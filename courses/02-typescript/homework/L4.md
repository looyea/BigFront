# TS L4 作业 · 泛型

1. 写泛型函数 `last<T>(arr: T[]): T | undefined`。
2. 写泛型接口 `ApiResponse<T>`：包含 `data: T` 与 `code: number`。
3. 用约束实现 `pluck<T, K extends keyof T>(arr: T[], key: K): T[K][]`。
4.（挑战）解释为什么 `Record<K extends keyof any, V>` 里用了泛型约束。
