# TS L5 作业 · 工程

1. 写出你会在项目里用的最小 `tsconfig.json`（含 strict）。
2. 用 Pick/Omit 从 `interface Order` 派生"列表页只读字段类型"和"创建表单类型"。
3. 用 `npx tsc --noEmit` 检查本包 examples 目录，报告有无错误。
4.（挑战）为你已有的一个 JS 小工具写 `.d.ts` 类型声明。
