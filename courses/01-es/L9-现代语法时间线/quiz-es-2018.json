{
  "title": "ES2018 · 小测",
  "passRule": "答对 ≥60% 即通过",
  "questions": [
    {
      "id": "q1",
      "type": "single",
      "prompt": "以下代码 `publicUser` 里有什么？\n\n```js\nconst user = { id: 1, name: 'Ann', pwd: 'x' };\nconst { pwd, ...publicUser } = user;\n```",
      "options": [
        "{ id: 1, name: 'Ann', pwd: 'x' }",
        "{ pwd: 'x' }",
        "抛错",
        "{ id: 1, name: 'Ann' }"
      ],
      "answer": 3,
      "explanation": "rest 收集**剩余**可枚举自有键——pwd 被解构走，publicUser = { id, name }。这是**浅拷贝**。"
    },
    {
      "id": "q2",
      "type": "single",
      "prompt": "对象 spread `{ ...obj }` 与 `Object.assign({}, obj)` 的**主要**差别：",
      "options": [
        "完全等价",
        "spread 是**语法**、无法被 Proxy set trap 拦截；assign 会走 set 触发拦截",
        "spread 支持 Symbol，assign 不支持",
        "assign 支持 getter，spread 不支持"
      ],
      "answer": 1,
      "explanation": "语义相近，但 Object.assign 触发 target 的 setter（可被 Proxy 拦截）；spread 走 CreateDataProperty（**不**触发 setter）。"
    },
    {
      "id": "q3",
      "type": "single",
      "prompt": "`for await...of` 消费的是：",
      "options": [
        "普通数组",
        "AsyncIterable（`Symbol.asyncIterator`）——每次 next 返回 Promise",
        "同步 Generator",
        "只要实现了 `then` 的对象"
      ],
      "answer": 1,
      "explanation": "for-await 每次 next() 得到 Promise<{value,done}>——自动 await。Node stream、fetch body、async generator 都符合。"
    },
    {
      "id": "q4",
      "type": "single",
      "prompt": "以下代码打印什么？\n\n```js\nPromise.resolve('ok')\n  .finally(() => console.log('F'))\n  .then(v => console.log(v));\n```",
      "options": [
        "F → ok（finally 不吞值）",
        "只打 F",
        "只打 ok",
        "抛错"
      ],
      "answer": 0,
      "explanation": "finally **不接参数、不改变状态**——上游的 value 原样穿透。所以先打 F（清理），再打 ok。"
    },
    {
      "id": "q5",
      "type": "single",
      "prompt": "命名捕获组的值挂在哪个字段？\n\n```js\n/(?<y>\\d{4})/.exec('2026')\n```",
      "options": [
        "`m[0]`",
        "`m.groups.y`",
        "`m.named.y`",
        "`m.captures.y`"
      ],
      "answer": 1,
      "explanation": "匹配结果的 `groups` 字段是所有命名捕获的容器；`m[1]` 依然能用（索引）。"
    },
    {
      "id": "q6",
      "type": "single",
      "prompt": "正则 `s` 标志（dotAll）的作用是：",
      "options": [
        "开启严格模式",
        "让 `.` 匹配**换行符** `\\n`",
        "开启 sticky 模式",
        "多行匹配"
      ],
      "answer": 1,
      "explanation": "默认 `.` 不匹配 `\\n` — 加了 s 就可以。终于不用写 `[\\s\\S]`。sticky 是 `y`，多行是 `m`。"
    },
    {
      "id": "q7",
      "type": "single",
      "prompt": "以下**不能**被 Babel 降级的是：",
      "options": [
        "对象 spread",
        "for-await-of",
        "lookbehind `(?<=x)`",
        "Promise.finally"
      ],
      "answer": 2,
      "explanation": "lookbehind 是**引擎级**正则语法——Babel 处理 JS AST 不改正则内部；Safari 16.4 前不支持。其它三项 babel 都能降级（Promise.finally 靠 core-js polyfill）。"
    },
    {
      "id": "q8",
      "type": "single",
      "prompt": "`p.then(v => v).finally(() => { throw new Error(\"boom\") })` 的结果：",
      "options": [
        "fulfilled 原值",
        "rejected（boom 接管链条）",
        "uncaught 异常被忽略",
        "finally 不执行"
      ],
      "answer": 1,
      "explanation": "finally 回调抛错会让派生 promise 变 rejected——它**透传**前后值但抛错同样进错误通道；这是做「统一失败上抛」的合法技巧。"
    },
    {
      "id": "q9",
      "type": "single",
      "prompt": "`async function* gen() { yield 1 }` 调用后 `gen().next()` 返回：",
      "options": [
        "{ value: 1, done: false }",
        "Promise<{value:1,done:false}>",
        "抛错",
        "{ value: Promise<1> }"
      ],
      "answer": 1,
      "explanation": "async 生成器的 next() 一律返回 Promise（值再包一层 await 语义），配合 for-await 消费——这是「异步迭代器协议」的形态。"
    },
    {
      "id": "q10",
      "type": "single",
      "prompt": "`\"一\".match(/\\p{Script=Han}/)`（没加 u 旗标）的行为：",
      "options": [
        "匹配中文",
        "不匹配中文——\\p 被当作字面量字符 p",
        "SyntaxError",
        "返回 null 并告警"
      ],
      "answer": 1,
      "explanation": "\\p{...} Unicode 属性类要求 u（或 v）旗标；无旗标时 \\p 退化为普通字母 p 的转义。忘了 u 是 Unicode 正则最隐蔽的坑。"
    }
  ]
}
