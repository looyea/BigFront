# L4 作业 · 模块化

1. 新建 `utils.js`，命名导出 `sum` 与 `PI`，再在另一个文件里用两种方式（命名 + 命名空间）导入使用。
2. 说明默认导出与命名导出各自的适用场景，各写一个例子。
3. 解释为什么本项目要写 `"type": "module"`，去掉后 `import` 会怎样。
4.（挑战）写一个 `counter.js` 验证"模块单例"：两个不同文件 import 同一个 count，修改后另一处能看到吗？
