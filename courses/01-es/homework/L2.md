# L2 作业 · 函数进化

1. 用箭头函数重写：`function double(x){ return x*2 }`，并写成"隐式返回"一行版。
2. 解释 `const f = () => { a: 1 }` 为什么返回 undefined，并给出修复写法。
3. 写一个带默认参数的 `connect({ host = "127.0.0.1", port = 5432 } = {})`，打印不同调用结果。
4. 用 rest 参数实现 `average(...nums)`，返回平均值；思考：`average([1,2,3])` 能得到期望结果吗？为什么？
5.（挑战）说明为什么 `setInterval(function(){...}, 1000)` 里的 this 会丢，改用箭头函数解决。
