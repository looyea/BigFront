# L6 作业 · 现代语法巡礼

1. 用 `?.` 和 `??` 重写：`var city = (user && user.address && user.address.city) || "未知";`，思考 `||` 换 `??` 的差异。
2. 用 `Array.prototype.at` 取数组倒数第二个元素。
3. 写一个含公有字段与私有 `#` 字段的 class，演示私有字段无法从外部访问。
4. 用 `config.timeout ??= 3000` 说明逻辑赋值等价的完整写法。
5.（信息素养）到 MDN 查证 `Object.groupBy`（ES2024）是否存在并写出用法，杜绝道听途说。
