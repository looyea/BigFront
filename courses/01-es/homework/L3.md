# L3 作业 · 数据拆解

1. 从 `{ user: { name: "K", tags: ["a","b"] } }` 中解构出 `name` 和第二个 tag。
2. 不借助临时变量交换两个变量的值。
3. 用展开运算符基于 `const base = {a:1,b:2}` 生成 `{a:1,b:3,c:4}`。
4. 判断 `{...obj}` 是深拷贝还是浅拷贝，并写代码用 `structuredClone` 修正嵌套修改互相影响的问题。
5.（挑战）写一个 `renameKey({ oldKey, newKey }, obj)` 工具，用解构 + 计算属性生成新对象。
