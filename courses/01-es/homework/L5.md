# L5 作业 · 异步编程

1. 把三层嵌套回调改写成 async/await + try/catch。
2. 用 `Promise.all` 并发获取 3 个模拟接口（用 setTimeout 包 Promise），要求总耗时约等于最慢的一个。
3. 说明 `Promise.all` 与 `Promise.allSettled` 的差别，什么场景必须用后者。
4. 找出 bug 并改正：`for (const u of users) arr.push(fetchUser(u).then(r=>r.json()))` 后立刻读取 arr 里的数据。
5.（挑战）实现 `retry(fn, times)`：fn 返回 Promise，失败时最多重试 times 次。
